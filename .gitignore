/target
.env
*.db

# Live deployment secrets — never commit.
deploy/stellargate.env

# Schema snapshot is generated by tests/schema_snapshot_test.rs and should
# not be committed — it is an output artefact, not a source file.
tests/schema_snapshot.sql

# Editor and OS noise.
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE directories.
.idea/
.vscode/
*.iml

# Compiled/generated artefacts.
*.pdb
